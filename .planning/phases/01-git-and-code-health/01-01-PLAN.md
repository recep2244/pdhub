---
phase: 01-git-and-code-health
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/protein_design_hub/agents/mutagenesis_agents.py
  - src/protein_design_hub/web/pages/10_mutation_scanner.py.bak
  - .gitignore
autonomous: true
requirements:
  - GIT-01
  - GIT-02

must_haves:
  truths:
    - "`git status` shows `mutagenesis_agents.py` as tracked (no `??` prefix)"
    - "`10_mutation_scanner.py.bak` does not exist on disk"
    - "`*.bak` is listed in `.gitignore`"
    - "All 17 existing pipeline integrity tests still pass after committing mutagenesis_agents.py"
  artifacts:
    - path: "src/protein_design_hub/agents/mutagenesis_agents.py"
      provides: "Mutagenesis agent module — tracked in git"
      contains: "class MutagenesiReportAgent"
    - path: ".gitignore"
      provides: "Prevents future .bak files from appearing as untracked"
      contains: "*.bak"
  key_links:
    - from: "src/protein_design_hub/agents/orchestrator.py"
      to: "src/protein_design_hub/agents/mutagenesis_agents.py"
      via: "lazy import inside _build_mutagenesis_post_approval_pipeline"
      pattern: "from protein_design_hub.agents.mutagenesis_agents import"
    - from: "src/protein_design_hub/agents/registry.py"
      to: "src/protein_design_hub/agents/mutagenesis_agents.py"
      via: "lazy import inside _register_mutagenesis_agents"
      pattern: "from protein_design_hub.agents.mutagenesis_agents import"
---

<objective>
Commit the untracked `mutagenesis_agents.py` to git and remove the orphaned `.bak` backup file.

Purpose: A fresh clone currently breaks the mutagenesis pipeline because `mutagenesis_agents.py` is missing from git history. The `.bak` file pollutes `git status` with untracked noise.
Output: Clean `git status` — mutagenesis module tracked, backup gone, `*.bak` gitignored.
</objective>

<execution_context>
@/home/recep/.claude/get-shit-done/workflows/execute-plan.md
@/home/recep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-git-and-code-health/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit mutagenesis_agents.py to git (GIT-01)</name>
  <files>src/protein_design_hub/agents/mutagenesis_agents.py</files>
  <action>
    Verify import paths before committing:
    Run `grep -rn "from.*mutagenesis_agents" src/ tests/` — expected output is ONLY orchestrator.py and registry.py. If any web page or CLI file imports it directly, investigate before proceeding.

    Run a quick syntax check: `python -c "import sys; sys.path.insert(0, 'src'); import protein_design_hub.agents.mutagenesis_agents; print('OK')"` from the project root. If this fails with ImportError or SyntaxError, do NOT commit — fix the error first.

    Stage and commit the file:
    `git add src/protein_design_hub/agents/mutagenesis_agents.py`

    Commit with message:
    `git commit -m "chore: track mutagenesis_agents.py in version control

    Adds the untracked computational mutagenesis agents module to git.
    Imports verified: only orchestrator.py and registry.py reference this
    module via lazy imports inside _build_mutagenesis_post_approval_pipeline
    and _register_mutagenesis_agents. Pipeline integrity tests confirmed.

    Closes: GIT-01"`

    After committing, run `pytest tests/test_agent_pipeline_integrity.py -v` to confirm all 17 tests still pass. If tests fail, investigate — the commit alone should not change runtime behavior since the working-tree file was already visible to Python.
  </action>
  <verify>
    1. `git status` no longer shows `?? src/protein_design_hub/agents/mutagenesis_agents.py`
    2. `git log --oneline -1` shows the commit
    3. `pytest tests/test_agent_pipeline_integrity.py -v` — all tests pass (expect 17+ passing)
  </verify>
  <done>
    `mutagenesis_agents.py` appears as a tracked file in `git log`. `pytest tests/test_agent_pipeline_integrity.py` exits 0 with no failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete .bak backup and add *.bak to .gitignore (GIT-02)</name>
  <files>
    src/protein_design_hub/web/pages/10_mutation_scanner.py.bak
    .gitignore
  </files>
  <action>
    First, confirm the .bak is safe to delete by diffing it against the current file. Run:
    `diff src/protein_design_hub/web/pages/10_mutation_scanner.py.bak src/protein_design_hub/web/pages/10_mutation_scanner.py | head -80`

    Expected: the diff shows the current file has MORE content (additions visible as `>` or `+` lines). If any content appears ONLY in the .bak (lines marked `<` with no matching `>` counterpart), STOP and report what was found before deleting.

    Research confirmed the current file is a superset: it contains `_render_manual_tab_settings()`, `_EXAMPLE_SEQUENCES`, and "Change Sequence" button additions not in the .bak. Proceed with deletion if diff confirms this.

    Delete the .bak file:
    `rm src/protein_design_hub/web/pages/10_mutation_scanner.py.bak`

    Add `*.bak` to .gitignore. Find the "Temporary files" section in .gitignore (or the end of the file if no such section exists). Append:
    ```
    *.bak
    ```
    Use the Read tool to check .gitignore first, then Write to add the entry in a logical location (after the existing `env.bak/` and `venv.bak/` entries, or in a new "Temporary files" comment block).

    Stage and commit:
    `git add .gitignore`

    Note: The .bak file was untracked, so no `git rm` is needed — `rm` alone removes it from disk, and the .gitignore entry prevents it from showing as untracked in the future.

    Commit with message:
    `git commit -m "chore: delete orphaned .bak backup and ignore *.bak files

    10_mutation_scanner.py.bak (Feb 10 15:56) confirmed safe to remove —
    current file (Feb 10 15:58) is a superset containing all .bak content
    plus _render_manual_tab_settings() refactor and example sequence loader.
    Added *.bak to .gitignore to prevent future backup noise.

    Closes: GIT-02"`
  </action>
  <verify>
    1. `ls src/protein_design_hub/web/pages/10_mutation_scanner.py.bak 2>/dev/null` returns nothing (file does not exist)
    2. `grep "*.bak" .gitignore` matches (entry is present)
    3. `git status` shows no `??` entry for any .bak file
    4. `git log --oneline -1` shows the .gitignore commit
  </verify>
  <done>
    `10_mutation_scanner.py.bak` does not exist. `.gitignore` contains `*.bak`. `git status` is clean of any .bak entries.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `git status` — working tree is clean except for other existing modified files (the M-prefixed files from the original git status are unrelated to this plan)
- `git log --oneline -2` — shows both commits (mutagenesis_agents.py + .gitignore)
- `pytest tests/test_agent_pipeline_integrity.py -v` — all tests pass
- `grep "*.bak" .gitignore` — entry confirmed present
- `ls src/protein_design_hub/web/pages/*.bak 2>/dev/null` — no output (no .bak files exist)
</verification>

<success_criteria>
- `mutagenesis_agents.py` is in git history (`git log -- src/protein_design_hub/agents/mutagenesis_agents.py` shows a commit)
- `10_mutation_scanner.py.bak` no longer exists on disk
- `.gitignore` contains `*.bak` entry
- All pipeline integrity tests pass after these changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-git-and-code-health/01-01-SUMMARY.md`
</output>
