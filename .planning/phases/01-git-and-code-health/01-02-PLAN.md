---
phase: 01-git-and-code-health
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/protein_design_hub/agents/mutagenesis_agents.py
  - src/protein_design_hub/agents/orchestrator.py
  - src/protein_design_hub/agents/registry.py
  - tests/test_agent_pipeline_integrity.py
autonomous: true
requirements:
  - GIT-03

must_haves:
  truths:
    - "`MutagenesisPipelineReportAgent` is the class definition name in mutagenesis_agents.py"
    - "`MutagenesiReportAgent` is importable from mutagenesis_agents as a backward-compat alias"
    - "orchestrator.py and registry.py import and use `MutagenesisPipelineReportAgent` (canonical name)"
    - "All 18 pipeline integrity tests pass (17 existing + 1 new import alias test)"
  artifacts:
    - path: "src/protein_design_hub/agents/mutagenesis_agents.py"
      provides: "Renamed class with backward-compatible alias"
      contains: "class MutagenesisPipelineReportAgent"
    - path: "src/protein_design_hub/agents/orchestrator.py"
      provides: "Updated import using canonical class name"
      contains: "MutagenesisPipelineReportAgent"
    - path: "src/protein_design_hub/agents/registry.py"
      provides: "Updated import and register call using canonical class name"
      contains: "MutagenesisPipelineReportAgent"
    - path: "tests/test_agent_pipeline_integrity.py"
      provides: "Import alias test confirming both names are valid"
      contains: "test_mutagenesis_report_agent_importable_by_both_names"
  key_links:
    - from: "src/protein_design_hub/agents/mutagenesis_agents.py"
      to: "MutagenesiReportAgent alias"
      via: "module-level assignment after class definition"
      pattern: "MutagenesiReportAgent = MutagenesisPipelineReportAgent"
    - from: "src/protein_design_hub/agents/orchestrator.py"
      to: "src/protein_design_hub/agents/mutagenesis_agents.py"
      via: "lazy import: MutagenesisPipelineReportAgent"
      pattern: "MutagenesisPipelineReportAgent"
    - from: "tests/test_agent_pipeline_integrity.py"
      to: "src/protein_design_hub/agents/mutagenesis_agents.py"
      via: "import test asserting identity of both names"
      pattern: "MutagenesiReportAgent is MutagenesisPipelineReportAgent"
---

<objective>
Rename the typo class `MutagenesiReportAgent` to the canonical `MutagenesisPipelineReportAgent`, add a silent backward-compatible alias, update all call sites, and add an import test.

Purpose: The class name typo (`Mutagenesi` vs `Mutagenesis`) is inconsistent with conventions and creates friction during refactoring. The canonical name must match the agent's role in the pipeline.
Output: Three updated source files with canonical name; one new test confirming both names import correctly.
</objective>

<execution_context>
@/home/recep/.claude/get-shit-done/workflows/execute-plan.md
@/home/recep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-git-and-code-health/01-RESEARCH.md
@.planning/phases/01-git-and-code-health/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename class and add backward-compatible alias in mutagenesis_agents.py</name>
  <files>src/protein_design_hub/agents/mutagenesis_agents.py</files>
  <action>
    Read the file first with the Read tool to locate the class definition precisely (research says line 387).

    STEP 1 — Rename the class definition:
    Find `class MutagenesiReportAgent(BaseAgent):` (note: only ONE `s` in `Mutagenesi`) and rename it to `class MutagenesisPipelineReportAgent(BaseAgent):`.

    Do NOT change anything else in the class body. The `name = "mutagenesis_report"` attribute inside the class stays exactly as-is — that is the runtime agent key, not the Python class name.

    STEP 2 — Add the alias immediately after the class ends:
    Immediately after the last line of the `MutagenesisPipelineReportAgent` class body (which research says ends around line 559), add exactly:

    ```python
    # Backward-compatible alias — MutagenesiReportAgent was a typo (missing 's')
    MutagenesiReportAgent = MutagenesisPipelineReportAgent
    ```

    The alias must be at module level (not indented inside the class). It must come BEFORE any other code that follows the class.

    STEP 3 — Quick syntax check:
    Run `python -c "import sys; sys.path.insert(0, 'src'); from protein_design_hub.agents.mutagenesis_agents import MutagenesisPipelineReportAgent, MutagenesiReportAgent; assert MutagenesiReportAgent is MutagenesisPipelineReportAgent; print('OK')"` from the project root. Both names must import without error and must be the same object.

    If this passes, both names are live in the module — it is now safe to update call sites in the next task.
  </action>
  <verify>
    `python -c "import sys; sys.path.insert(0, 'src'); from protein_design_hub.agents.mutagenesis_agents import MutagenesisPipelineReportAgent, MutagenesiReportAgent; assert MutagenesiReportAgent is MutagenesisPipelineReportAgent; print('ALIAS OK')"` prints `ALIAS OK`.
  </verify>
  <done>
    `mutagenesis_agents.py` defines `class MutagenesisPipelineReportAgent` and exports `MutagenesiReportAgent` as a module-level alias pointing to the same class. Both names are importable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update call sites in orchestrator.py and registry.py, add import test</name>
  <files>
    src/protein_design_hub/agents/orchestrator.py
    src/protein_design_hub/agents/registry.py
    tests/test_agent_pipeline_integrity.py
  </files>
  <action>
    Read both orchestrator.py and registry.py with the Read tool before editing. Research identifies exact lines:
    - orchestrator.py: import at lines 145-150, instantiation at line 160
    - registry.py: import at lines 89-93, register call at line 96

    UPDATE orchestrator.py:
    In the lazy import block inside `_build_mutagenesis_post_approval_pipeline`, change:
    ```python
    MutagenesiReportAgent,
    ```
    to:
    ```python
    MutagenesisPipelineReportAgent,
    ```
    Also update the instantiation line:
    ```python
    MutagenesiReportAgent(progress_callback=progress_callback),
    ```
    to:
    ```python
    MutagenesisPipelineReportAgent(progress_callback=progress_callback),
    ```

    UPDATE registry.py:
    In the lazy import block inside `_register_mutagenesis_agents`, change:
    ```python
    MutagenesiReportAgent,
    ```
    to:
    ```python
    MutagenesisPipelineReportAgent,
    ```
    Also update the register call:
    ```python
    AgentRegistry.register("mutagenesis_report", MutagenesiReportAgent)
    ```
    to:
    ```python
    AgentRegistry.register("mutagenesis_report", MutagenesisPipelineReportAgent)
    ```
    Note: The string key `"mutagenesis_report"` does NOT change — only the Python class name reference changes.

    ADD import test in tests/test_agent_pipeline_integrity.py:
    Read the test file first to find where to add the new test (append after the last existing test function). Add:

    ```python
    def test_mutagenesis_report_agent_importable_by_both_names():
        """Canonical class and backward-compat alias must both be importable."""
        from protein_design_hub.agents.mutagenesis_agents import (
            MutagenesisPipelineReportAgent,
            MutagenesiReportAgent,
        )
        assert MutagenesiReportAgent is MutagenesisPipelineReportAgent
    ```

    After all edits, run the full test suite:
    `pytest tests/test_agent_pipeline_integrity.py -v`

    Expect 18 tests to pass (17 original + 1 new). If any test fails, read the error carefully:
    - `ImportError: cannot import name 'MutagenesisPipelineReportAgent'` → the alias in mutagenesis_agents.py is wrong; fix Task 1 output
    - Any pipeline structure test failure → a call site was updated incorrectly; verify orchestrator.py and registry.py changes

    Once all 18 tests pass, commit all four files atomically:
    `git add src/protein_design_hub/agents/mutagenesis_agents.py src/protein_design_hub/agents/orchestrator.py src/protein_design_hub/agents/registry.py tests/test_agent_pipeline_integrity.py`

    Commit with message:
    `git commit -m "refactor: rename MutagenesiReportAgent to MutagenesisPipelineReportAgent

    Fixes class name typo (missing 's' in Mutagenesis). Canonical name is
    MutagenesisPipelineReportAgent. Silent backward-compatible alias preserved
    in mutagenesis_agents.py for any external references. Call sites updated
    in orchestrator.py and registry.py. Import alias test added.

    All 18 pipeline integrity tests pass.

    Closes: GIT-03"`
  </action>
  <verify>
    1. `grep -n "MutagenesiReportAgent" src/protein_design_hub/agents/orchestrator.py` returns no lines (old name is gone from call sites)
    2. `grep -n "MutagenesiReportAgent" src/protein_design_hub/agents/registry.py` returns no lines (old name is gone from call sites)
    3. `grep -n "MutagenesisPipelineReportAgent" src/protein_design_hub/agents/orchestrator.py` returns at least 2 lines (import + instantiation)
    4. `grep -n "MutagenesisPipelineReportAgent" src/protein_design_hub/agents/registry.py` returns at least 2 lines (import + register)
    5. `pytest tests/test_agent_pipeline_integrity.py -v` exits 0 with 18 tests passing
    6. `git log --oneline -1` shows the refactor commit
  </verify>
  <done>
    All four files committed. 18 tests pass. `orchestrator.py` and `registry.py` use only the canonical `MutagenesisPipelineReportAgent` name. `mutagenesis_agents.py` exports both names. The new import test asserts identity of both names.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep -rn "class MutagenesisPipelineReportAgent" src/` — finds exactly 1 match in mutagenesis_agents.py
- `grep -rn "MutagenesiReportAgent = " src/` — finds exactly 1 match (the alias line) in mutagenesis_agents.py
- `grep -rn "MutagenesiReportAgent" src/protein_design_hub/agents/orchestrator.py` — 0 matches (old name gone from call site)
- `grep -rn "MutagenesiReportAgent" src/protein_design_hub/agents/registry.py` — 0 matches (old name gone from call site)
- `pytest tests/test_agent_pipeline_integrity.py -v` — 18 tests pass, including `test_mutagenesis_report_agent_importable_by_both_names`
- `git log --oneline -3` — shows all three commits from this phase (GIT-01, GIT-02, GIT-03)
</verification>

<success_criteria>
- `MutagenesisPipelineReportAgent` is the class definition in mutagenesis_agents.py
- `MutagenesiReportAgent = MutagenesisPipelineReportAgent` alias exists at module level
- orchestrator.py and registry.py import only `MutagenesisPipelineReportAgent` (no old typo name in call sites)
- `pytest tests/test_agent_pipeline_integrity.py` passes with 18 tests (was 17)
- Both class names are importable and `assert MutagenesiReportAgent is MutagenesisPipelineReportAgent` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-git-and-code-health/01-02-SUMMARY.md`
</output>
